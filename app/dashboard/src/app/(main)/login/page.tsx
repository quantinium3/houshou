import {LoginForm} from "@/components/login-form"

export default function Login() {
    return (
            <div className="login flex justify-center items-center w-full mt-9">
                <LoginForm />
            </div>
    );
}